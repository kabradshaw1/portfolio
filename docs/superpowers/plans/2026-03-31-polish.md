# Polish & Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add enhanced health checks (Qdrant + Ollama connectivity), improve error handling (503s when services unreachable), and sync the Windows machine's repo.

**Architecture:** Modify both services' `/health` endpoints to check Qdrant and Ollama connectivity. Add try/except around external service calls in `/ingest` and `/chat` to return 503 instead of 500. Sync changes to Windows and verify.

**Tech Stack:** FastAPI, httpx, qdrant-client, pytest

---

## File Structure

```
services/
├── ingestion/
│   ├── app/
│   │   └── main.py          # Modify: /health endpoint, /ingest error handling
│   └── tests/
│       └── test_main.py     # Modify: add health check and error handling tests
├── chat/
│   ├── app/
│   │   └── main.py          # Modify: /health endpoint, /chat error handling
│   └── tests/
│       └── test_main.py     # Modify: add health check and error handling tests
```

---

### Task 1: Enhanced Health Check — Ingestion Service

**Files:**
- Modify: `services/ingestion/app/main.py`
- Modify: `services/ingestion/tests/test_main.py`

- [ ] **Step 1: Write failing tests for enhanced health check**

Add to `services/ingestion/tests/test_main.py`:

```python
@patch("app.main.httpx.AsyncClient")
@patch("app.main.QdrantClient")
def test_health_all_services_connected(mock_qdrant_cls, mock_httpx_cls):
    mock_qdrant = MagicMock()
    mock_qdrant.get_collections.return_value = True
    mock_qdrant_cls.return_value = mock_qdrant

    mock_client = AsyncMock()
    mock_client.get.return_value = AsyncMock(status_code=200)
    mock_httpx_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_httpx_cls.return_value.__aexit__ = AsyncMock(return_value=False)

    response = client.get("/health")
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "healthy"
    assert data["qdrant"] == "connected"
    assert data["ollama"] == "connected"


@patch("app.main.httpx.AsyncClient")
@patch("app.main.QdrantClient")
def test_health_qdrant_down(mock_qdrant_cls, mock_httpx_cls):
    mock_qdrant = MagicMock()
    mock_qdrant.get_collections.side_effect = Exception("Connection refused")
    mock_qdrant_cls.return_value = mock_qdrant

    mock_client = AsyncMock()
    mock_client.get.return_value = AsyncMock(status_code=200)
    mock_httpx_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_httpx_cls.return_value.__aexit__ = AsyncMock(return_value=False)

    response = client.get("/health")
    assert response.status_code == 503
    data = response.json()
    assert data["status"] == "degraded"
    assert data["qdrant"] == "disconnected"
    assert data["ollama"] == "connected"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd services/ingestion && python -m pytest tests/test_main.py -v`
Expected: FAIL — new tests fail because `/health` still returns static `{"status": "ok"}`

- [ ] **Step 3: Implement enhanced health check**

Replace the `/health` endpoint in `services/ingestion/app/main.py`. Add `import httpx` and `from qdrant_client import QdrantClient` at the top, then replace the health function:

```python
import httpx
from qdrant_client import QdrantClient

@app.get("/health")
async def health():
    qdrant_ok = False
    ollama_ok = False

    try:
        qclient = QdrantClient(host=settings.qdrant_host, port=settings.qdrant_port, timeout=3)
        qclient.get_collections()
        qdrant_ok = True
    except Exception:
        pass

    try:
        async with httpx.AsyncClient() as client:
            resp = await client.get(f"{settings.ollama_base_url}/api/tags", timeout=3.0)
            if resp.status_code == 200:
                ollama_ok = True
    except Exception:
        pass

    status = "healthy" if (qdrant_ok and ollama_ok) else "degraded"
    status_code = 200 if (qdrant_ok and ollama_ok) else 503

    from fastapi.responses import JSONResponse
    return JSONResponse(
        status_code=status_code,
        content={
            "status": status,
            "qdrant": "connected" if qdrant_ok else "disconnected",
            "ollama": "connected" if ollama_ok else "disconnected",
        },
    )
```

- [ ] **Step 4: Update the existing health test**

The existing `test_health` test needs to be updated since it expects a simple 200. Replace it:

```python
@patch("app.main.httpx.AsyncClient")
@patch("app.main.QdrantClient")
def test_health(mock_qdrant_cls, mock_httpx_cls):
    mock_qdrant = MagicMock()
    mock_qdrant.get_collections.return_value = True
    mock_qdrant_cls.return_value = mock_qdrant

    mock_client = AsyncMock()
    mock_client.get.return_value = AsyncMock(status_code=200)
    mock_httpx_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_httpx_cls.return_value.__aexit__ = AsyncMock(return_value=False)

    response = client.get("/health")
    assert response.status_code == 200
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `cd services/ingestion && python -m pytest tests/test_main.py -v`
Expected: All tests PASS

- [ ] **Step 6: Commit**

```bash
git add services/ingestion/app/main.py services/ingestion/tests/test_main.py
git commit -m "feat: add enhanced health checks to ingestion service (Qdrant + Ollama)"
```

---

### Task 2: Enhanced Health Check — Chat Service

**Files:**
- Modify: `services/chat/app/main.py`
- Modify: `services/chat/tests/test_main.py`

- [ ] **Step 1: Write failing tests for enhanced health check**

Add to `services/chat/tests/test_main.py`:

```python
from unittest.mock import AsyncMock, MagicMock, patch


@patch("app.main.httpx.AsyncClient")
@patch("app.main.QdrantClient")
def test_health_all_services_connected(mock_qdrant_cls, mock_httpx_cls):
    mock_qdrant = MagicMock()
    mock_qdrant.get_collections.return_value = True
    mock_qdrant_cls.return_value = mock_qdrant

    mock_client = AsyncMock()
    mock_client.get.return_value = AsyncMock(status_code=200)
    mock_httpx_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_httpx_cls.return_value.__aexit__ = AsyncMock(return_value=False)

    response = client.get("/health")
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "healthy"
    assert data["qdrant"] == "connected"
    assert data["ollama"] == "connected"


@patch("app.main.httpx.AsyncClient")
@patch("app.main.QdrantClient")
def test_health_ollama_down(mock_qdrant_cls, mock_httpx_cls):
    mock_qdrant = MagicMock()
    mock_qdrant.get_collections.return_value = True
    mock_qdrant_cls.return_value = mock_qdrant

    mock_client = AsyncMock()
    mock_client.get.side_effect = Exception("Connection refused")
    mock_httpx_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_httpx_cls.return_value.__aexit__ = AsyncMock(return_value=False)

    response = client.get("/health")
    assert response.status_code == 503
    data = response.json()
    assert data["status"] == "degraded"
    assert data["ollama"] == "disconnected"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd services/chat && python -m pytest tests/test_main.py -v`
Expected: FAIL — new tests fail

- [ ] **Step 3: Implement enhanced health check**

Replace the `/health` endpoint in `services/chat/app/main.py`. Add `import httpx` and `from qdrant_client import QdrantClient` at the top, then replace the health function:

```python
import httpx
from qdrant_client import QdrantClient

@app.get("/health")
async def health():
    qdrant_ok = False
    ollama_ok = False

    try:
        qclient = QdrantClient(host=settings.qdrant_host, port=settings.qdrant_port, timeout=3)
        qclient.get_collections()
        qdrant_ok = True
    except Exception:
        pass

    try:
        async with httpx.AsyncClient() as client:
            resp = await client.get(f"{settings.ollama_base_url}/api/tags", timeout=3.0)
            if resp.status_code == 200:
                ollama_ok = True
    except Exception:
        pass

    status = "healthy" if (qdrant_ok and ollama_ok) else "degraded"
    status_code = 200 if (qdrant_ok and ollama_ok) else 503

    from fastapi.responses import JSONResponse
    return JSONResponse(
        status_code=status_code,
        content={
            "status": status,
            "qdrant": "connected" if qdrant_ok else "disconnected",
            "ollama": "connected" if ollama_ok else "disconnected",
        },
    )
```

- [ ] **Step 4: Update the existing health test**

Replace the existing `test_health` in `services/chat/tests/test_main.py`:

```python
@patch("app.main.httpx.AsyncClient")
@patch("app.main.QdrantClient")
def test_health(mock_qdrant_cls, mock_httpx_cls):
    mock_qdrant = MagicMock()
    mock_qdrant.get_collections.return_value = True
    mock_qdrant_cls.return_value = mock_qdrant

    mock_client = AsyncMock()
    mock_client.get.return_value = AsyncMock(status_code=200)
    mock_httpx_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_httpx_cls.return_value.__aexit__ = AsyncMock(return_value=False)

    response = client.get("/health")
    assert response.status_code == 200
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `cd services/chat && python -m pytest tests/test_main.py -v`
Expected: All tests PASS

- [ ] **Step 6: Commit**

```bash
git add services/chat/app/main.py services/chat/tests/test_main.py
git commit -m "feat: add enhanced health checks to chat service (Qdrant + Ollama)"
```

---

### Task 3: Error Handling — 503 for Unreachable Services

**Files:**
- Modify: `services/ingestion/app/main.py`
- Modify: `services/ingestion/tests/test_main.py`
- Modify: `services/chat/app/main.py`
- Modify: `services/chat/tests/test_main.py`

- [ ] **Step 1: Write failing test for ingestion 503 on Ollama failure**

Add to `services/ingestion/tests/test_main.py`:

```python
@patch("app.main.get_store")
@patch("app.main.embed_texts", new_callable=AsyncMock)
@patch("app.main.extract_pages")
def test_ingest_returns_503_when_ollama_unreachable(mock_extract, mock_embed, mock_get_store):
    mock_extract.return_value = [
        {"page_number": 1, "text": "Hello world. " * 100},
    ]
    mock_embed.side_effect = httpx.ConnectError("Connection refused")
    mock_store = MagicMock()
    mock_get_store.return_value = mock_store

    pdf_content = b"%PDF-1.4 fake content"
    response = client.post(
        "/ingest",
        files={"file": ("test.pdf", io.BytesIO(pdf_content), "application/pdf")},
    )

    assert response.status_code == 503
    assert "ollama" in response.json()["detail"].lower() or "embed" in response.json()["detail"].lower()
```

- [ ] **Step 2: Write failing test for chat 503 on Ollama failure**

Add to `services/chat/tests/test_main.py`:

```python
@patch("app.main.rag_query")
def test_chat_returns_503_when_backend_unreachable(mock_rag_query):
    async def failing_rag_query(**kwargs):
        raise httpx.ConnectError("Connection refused")
        yield  # make it a generator

    mock_rag_query.side_effect = failing_rag_query

    response = client.post(
        "/chat",
        json={"question": "What is this?"},
    )
    assert response.status_code == 503
```

- [ ] **Step 3: Add 503 error handling to ingestion /ingest endpoint**

In `services/ingestion/app/main.py`, add `import httpx` at the top (if not already there), then wrap the embed + store calls in the `/ingest` endpoint:

```python
@app.post("/ingest")
async def ingest(file: UploadFile = File(...)):
    if not file.filename or not file.filename.lower().endswith(".pdf"):
        raise HTTPException(status_code=422, detail="Only PDF files are accepted")

    content = await file.read()
    max_bytes = settings.max_file_size_mb * 1024 * 1024
    if len(content) > max_bytes:
        raise HTTPException(
            status_code=422,
            detail=f"File exceeds {settings.max_file_size_mb}MB limit",
        )

    try:
        pages = extract_pages(BytesIO(content))
    except ValueError as e:
        raise HTTPException(status_code=422, detail=str(e))

    chunks = chunk_pages(
        pages,
        chunk_size=settings.chunk_size,
        chunk_overlap=settings.chunk_overlap,
    )

    if not chunks:
        raise HTTPException(status_code=422, detail="No text content found in PDF")

    texts = [c["text"] for c in chunks]
    try:
        vectors = await embed_texts(
            texts=texts,
            ollama_base_url=settings.ollama_base_url,
            model=settings.embedding_model,
        )
    except (httpx.ConnectError, httpx.TimeoutException) as e:
        raise HTTPException(status_code=503, detail=f"Embedding service unavailable: {e}")

    document_id = str(uuid.uuid4())
    try:
        store = get_store()
        store.upsert(
            chunks=chunks,
            vectors=vectors,
            document_id=document_id,
            filename=file.filename,
        )
    except Exception as e:
        raise HTTPException(status_code=503, detail=f"Vector store unavailable: {e}")

    return {
        "status": "success",
        "document_id": document_id,
        "chunks_created": len(chunks),
        "filename": file.filename,
    }
```

- [ ] **Step 4: Add 503 error handling to chat /chat endpoint**

In `services/chat/app/main.py`, add `import httpx` at the top and wrap the endpoint:

```python
@app.post("/chat")
async def chat(request: ChatRequest):
    async def event_generator():
        try:
            async for event in rag_query(
                question=request.question,
                ollama_base_url=settings.ollama_base_url,
                chat_model=settings.chat_model,
                embedding_model=settings.embedding_model,
                qdrant_host=settings.qdrant_host,
                qdrant_port=settings.qdrant_port,
                collection_name=request.collection or settings.collection_name,
            ):
                yield {"data": json.dumps(event)}
        except (httpx.ConnectError, httpx.TimeoutException) as e:
            yield {"data": json.dumps({"error": f"Backend service unavailable: {e}"})}
        except Exception as e:
            yield {"data": json.dumps({"error": f"Internal error: {e}"})}

    try:
        # Test that we can start the pipeline before committing to SSE
        return EventSourceResponse(event_generator())
    except (httpx.ConnectError, httpx.TimeoutException):
        raise HTTPException(status_code=503, detail="Backend services unavailable")
```

- [ ] **Step 5: Add httpx import to test files**

Add `import httpx` to the top of both test files.

- [ ] **Step 6: Run all tests**

```bash
cd /Users/kylebradshaw/repos/gen_ai_engineer/services/ingestion && python -m pytest tests/ -v
cd /Users/kylebradshaw/repos/gen_ai_engineer/services/chat && python -m pytest tests/ -v
```

Expected: All tests pass in both services.

- [ ] **Step 7: Commit**

```bash
git add services/ingestion/app/main.py services/ingestion/tests/test_main.py services/chat/app/main.py services/chat/tests/test_main.py
git commit -m "feat: add 503 responses when Ollama or Qdrant are unreachable"
```

---

### Task 4: Sync and Verify on Windows

**Files:**
- No new files — operational task

- [ ] **Step 1: Copy updated service files to Windows**

```bash
scp services/ingestion/app/main.py PC@100.79.113.84:"C:\\Users\\PC\\repos\\gen_ai_engineer\\services\\ingestion\\app\\main.py"
scp services/chat/app/main.py PC@100.79.113.84:"C:\\Users\\PC\\repos\\gen_ai_engineer\\services\\chat\\app\\main.py"
scp docker-compose.yml PC@100.79.113.84:"C:\\Users\\PC\\repos\\gen_ai_engineer\\docker-compose.yml"
```

- [ ] **Step 2: Rebuild and restart services on Windows**

```bash
ssh PC@100.79.113.84 "cd C:\Users\PC\repos\gen_ai_engineer && docker compose up -d --build"
```

- [ ] **Step 3: Verify health endpoints show connectivity**

```bash
ssh PC@100.79.113.84 "curl -s http://localhost:8001/health"
```
Expected: `{"status":"healthy","qdrant":"connected","ollama":"connected"}`

```bash
ssh PC@100.79.113.84 "curl -s http://localhost:8002/health"
```
Expected: `{"status":"healthy","qdrant":"connected","ollama":"connected"}`

- [ ] **Step 4: Verify the full pipeline still works**

```bash
ssh PC@100.79.113.84 "curl -s -X POST http://localhost:8002/chat -H \"Content-Type: application/json\" -d \"{\\\"question\\\": \\\"What was the revenue?\\\"}\" --max-time 60"
```

Expected: SSE stream with tokens and sources.

- [ ] **Step 5: Commit**

```bash
git commit --allow-empty -m "chore: verified full stack on Windows with enhanced health checks"
```
