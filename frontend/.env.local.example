# Python AI services (ingestion, chat, debug)
NEXT_PUBLIC_API_URL=http://localhost:8000

# IR agent service (live demo mode). Falls back to NEXT_PUBLIC_API_URL, then
# localhost. The replay demo needs no backend — it reads static transcripts.
NEXT_PUBLIC_IR_AGENT_API_URL=http://localhost:8000

# Java GraphQL gateway (task management)
# Production (Vercel): https://api.kylebradshaw.dev
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080

# Google OAuth (for Java task management login)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
