apiVersion: batch/v1
kind: Job
metadata:
  name: go-cart-migrate
  namespace: go-ecommerce
spec:
  backoffLimit: 2
  activeDeadlineSeconds: 120
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: migrate
          image: ghcr.io/kabradshaw1/portfolio/go-cart-service:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              # Assemble the migrate-target DSN from component env vars.
              # The *_DIRECT host/port/options bypass PgBouncer because
              # golang-migrate uses session-level features that PgBouncer's
              # transaction-pool mode doesn't preserve.
              DATABASE_URL="postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST_DIRECT}:${DB_PORT_DIRECT}/${DB_NAME}?${DB_OPTIONS_DIRECT}"
              echo "Running cart-service migrations..."
              /usr/local/bin/migrate -path=/migrations -database="${DATABASE_URL}&x-migrations-table=cart_schema_migrations" up
              echo "Done."
          envFrom:
            - configMapRef:
                name: cart-service-config
            - secretRef:
                name: cart-service-db
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
