apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
  namespace: java-tasks
data:
  REDIS_HOST: redis
  RABBITMQ_HOST: rabbitmq
  RABBITMQ_USER: guest
  RABBITMQ_PASSWORD: guest
