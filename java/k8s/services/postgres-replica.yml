apiVersion: v1
kind: Service
metadata:
  name: postgres-replica
  namespace: java-tasks
spec:
  selector:
    app: postgres-replica
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
    - name: metrics
      port: 9187
      targetPort: 9187
