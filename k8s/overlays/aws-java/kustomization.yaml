apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../java/k8s

patches:
  - path: patches/remove-infra.yaml
  - path: patches/managed-services.yaml
  - path: patches/ingress-alb.yaml
